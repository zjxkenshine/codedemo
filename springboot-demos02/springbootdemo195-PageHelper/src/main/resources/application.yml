spring:
  # 配置数据源
  datasource:
    # 数据库驱动，这里配置高版本mysql驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/db3?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false
    username: root
    password: zjx123456

# pagehelper配置
pagehelper:
  # 方言配置
  helperDialect: mysql
  # 分页合理化
  reasonable: true
  # 是否支持接口参数来传递分页参数
  supportMethodsArguments: true
  params: count=countSql

# 日志
# LoggingApplicationListener
logging:
  level:
    com:
      kenshine:
        pagehelper:
          mapper: debug